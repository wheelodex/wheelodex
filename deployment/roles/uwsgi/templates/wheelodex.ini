[uwsgi]
socket = {{uwsgi_socket}}
env = WHEELODEX_CONFIG={{config_file}}
wsgi-file = {{wsgi_file}}
virtualenv = {{venv_path}}
manage-script-name = true
need-app = true

master = true
processes = {{uwsgi_processes}}
threads = {{uwsgi_threads}}
harakiri = 30
